# 隐私说明

本文描述当前 `qpm-thoughtline` 纯 Web Alpha 的默认数据处理方式。它不是法律意见；如果你把项目部署到自己的服务器，还需要为该部署补充适用的法律和日志说明。

## 默认不上传

应用核心没有账号系统、远程 API、内置云同步或遥测。任务、念头、节点和边默认只保存在本地浏览器中。

## Web 版本

- 快照保存在当前 origin 的 `localStorage`。
- 浏览器清理站点数据、隐私模式结束或存储配额不足都可能导致数据丢失。
- 设置页可以导出完整 JSON；导出的文件由操作系统和用户自己负责保护。
- 主题偏好也保存在浏览器本地。

## 剪贴板和 AI

“交给 AI”功能默认生成文本并尝试写入浏览器剪贴板。若当前浏览器不支持剪贴板 API 或未授予权限，复制会失败且应用不会自动发送文本到其他服务。用户主动把剪贴板内容粘贴到 Claude、ChatGPT、Codex、Cursor 或其他服务后，该服务按照自己的隐私政策处理内容。

## 通知

任务完成提醒使用浏览器通知。通知正文只包含任务标题和未处理数量，不应把秘密写入任务标题。用户可以在浏览器中撤销通知权限。

## 静态部署

如果使用 GitHub Pages 或其他静态托管平台，平台可能记录标准请求日志。该日志不由应用代码生成；日志保留周期和访问控制由对应平台负责。

## 用户责任

localStorage 不是加密保险箱。不要在念头中保存密码、令牌、私钥或其他不应进入普通浏览器配置目录的秘密。发现数据误传或安全问题时，请参阅 [SECURITY.md](SECURITY.md)。
