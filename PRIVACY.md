# 隐私说明

本文描述当前 `qpm-thoughtline` Web-first Alpha 的默认数据处理方式。它不是法律意见；如果你把项目部署到自己的服务器，还需要为该部署补充适用的法律和日志说明。

## 默认不上传

应用核心没有账号系统、远程 API、内置云同步或遥测。任务、念头、节点和边默认只保存在本地浏览器或 Tauri 应用数据目录中。

## Web 版本

- 快照保存在当前 origin 的 `localStorage`。
- 浏览器清理站点数据、隐私模式结束或存储配额不足都可能导致数据丢失。
- 设置页可以导出完整 JSON；导出的文件由操作系统和用户自己负责保护。
- 主题偏好也保存在浏览器本地。

## Tauri 版本

可选桌面壳使用本地 SQLite 文件，并请求全局快捷键、剪贴板和系统通知所需的权限。当前项目没有稳定桌面发行版，因此不同平台的实际数据路径和权限提示可能变化，发布时应以对应版本说明为准。

## 剪贴板和 AI

“交给 AI”功能默认生成文本并写入系统剪贴板。项目不会自动把文本发送到 AI 服务。用户主动把剪贴板内容粘贴到 Claude、ChatGPT、Codex、Cursor 或其他服务后，该服务按照自己的隐私政策处理内容。

## 通知

任务完成提醒可能使用浏览器通知或 Tauri 系统通知。通知正文只包含任务标题和未处理数量，不应把秘密写入任务标题。用户可以在操作系统或浏览器中撤销通知权限。

## 静态部署

如果你使用 Docker/Nginx 托管 Web 静态站点，Web 服务器可能记录标准访问日志。该日志不由应用代码生成；部署者应按自己的保留周期和访问控制策略管理它。

## 用户责任

localStorage 和本地 SQLite 都不是加密保险箱。不要在念头中保存密码、令牌、私钥或其他不应进入普通用户目录的秘密。发现数据误传或安全问题时，请参阅 [SECURITY.md](SECURITY.md)。
