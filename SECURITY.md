# Security Policy

## 支持范围

安全修复优先覆盖默认分支上最近的稳定版本。Alpha 版本不提供任何云端服务，但仍应认真处理本地数据暴露、导入文件解析、Tauri 权限和构建链问题。

## 报告漏洞

请通过仓库托管平台提供的私密安全报告入口联系维护者（GitHub Security Advisories，若仓库已启用）。如果该入口尚未启用，请先联系仓库维护者，不要把可利用细节直接发到公开 issue。

报告请尽量包含：

- 受影响的版本、操作系统和浏览器/桌面环境；
- 最小复现步骤或测试样例；
- 影响范围和可能的利用条件；
- 临时缓解方式；
- 是否包含真实数据（不要附带真实念头或秘密）。

请给维护者合理的修复时间。确认修复后，我们会在 CHANGELOG 或安全公告中记录影响和修复版本，不公开报告者不希望公开的细节。

## 数据边界

应用默认把数据留在浏览器 localStorage 或 Tauri SQLite。剪贴板和系统通知由用户主动使用或授权；将文本复制到第三方服务后，不再由本项目控制其处理方式。localStorage 和本地数据库都不是加密密钥存储。
